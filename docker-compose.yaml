services:
  db:
    image: postgres:18.3
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    container_name: teslodbtest
    volumes:
      - ./postgres:/var/lib/postgresql